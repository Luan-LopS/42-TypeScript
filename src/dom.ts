const campoNome = document.getElementById('nome')
const form = document.getElementsByTagName('form')
const btnEnviar = document.getElementById('btn-enviar')
btnEnviar?.addEventListener('click', (e:MouseEvent)=>{

})